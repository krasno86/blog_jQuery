-#= form_for(@articles.coment) do |f|
-#  - if coment.errors.any?
-#    #error_explanation
-#      %h2
-#        = pluralize(coment.errors.count, "error")
-#        prohibited this comment from being saved:
-#      %ul
-#        - comment.errors.full_messages.each do |message|
-#          %li= message
-#  .actions
-#    = f.submit
%p
  =coment.text
